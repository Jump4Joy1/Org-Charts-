{
  "name": "orgchart-builder",
  "version": "1.0.0",
  "private": true,
  "description": "OrgChart Builder — native app shells (Android/iOS) around the static web app.",
  "scripts": {
    "sync-www": "node scripts/sync-www.js",
    "cap:sync": "npm run sync-www && npx cap sync",
    "android:debug": "npm run cap:sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/cli": "8.4.2",
    "@capacitor/core": "8.4.2",
    "@capacitor/ios": "8.4.2"
  },
  "devDependencies": {
    "@capacitor/assets": "3.0.5"
  }
}
